/**
 * Route Conflict Resolution:
 * This file is deactivated to allow src/app/(dashboard)/page.tsx to serve as the primary route handler for /.
 * Next.js 15 does not allow two parallel pages to resolve to the same path.
 */
export const dynamic = 'force-static';
